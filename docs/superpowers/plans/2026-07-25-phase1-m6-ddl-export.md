# Phase 1 M6 — 논리 타입 완성·DDL 내보내기 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 방언 중립 논리 타입 17종의 파서·정규화를 완성하고, 4방언(PostgreSQL/MySQL·MariaDB/Oracle/MSSQL) 변환표를 구현하며, 그 위에 모델→DDL 생성기(CREATE TABLE·PK·FK·인덱스·코멘트·자동증가)를 만들어 에디터에서 방언·범위를 골라 DDL을 내보낸다.

**Architecture:** 타입 파서/변환/DDL 생성은 모두 `packages/core`의 순수 함수(IO 없음)로 둔다(02-architecture, CLI·서버 재사용). DDL은 문자열을 반환하고, 웹은 방언·범위 선택 후 그 문자열을 표시·복사·다운로드한다. 서버·DB 변경 없음. 도메인(Phase 2)·이미지(M8)·역설계(Phase 4)는 범위 밖.

**Tech Stack:** TypeScript, `@erdd/core`(순수 함수), React 19, vitest.

## Global Constraints

- 논리 타입 17종(14-domain 기준): `CHAR(n)`, `VARCHAR(n)`, `TEXT`, `SMALLINT`, `INT`, `BIGINT`, `DECIMAL(p,s)`, `FLOAT`, `DOUBLE`, `BOOLEAN`, `DATE`, `TIME`, `DATETIME`, `TIMESTAMPTZ`, `BLOB`, `JSON`, `UUID`.
- 관대한 파싱: 대소문자 무시, 별칭 수용(`INTEGER`→`INT`, `VARCHAR2`→`VARCHAR`, `NUMERIC`/`NUMBER`→`DECIMAL`, `TIMESTAMP`→`DATETIME`, `BOOL`→`BOOLEAN`, `DOUBLE PRECISION`→`DOUBLE`). 성공 시 **정규형** 저장(`varchar2(100)`→`VARCHAR(100)`). 실패 시 `{ ok:false, raw }`(원문 보존, DDL에 원문 그대로 출력).
- 파라미터 규칙: `CHAR`/`VARCHAR`는 길이 필수, `DECIMAL(p[,s])`은 s 생략 시 0, 그 외 타입에 괄호 파라미터가 오면 해석 실패.
- 방언 변환은 14-domain "기본 변환 표"를 **그대로** 따른다(아래 코드에 전량 수록). Oracle `TIME`→`TIMESTAMP`, Oracle `DATETIME`→`DATE`, Oracle `JSON`→`CLOB`는 경고를 동반한다.
- 자동증가: 정수 PK(`SMALLINT`/`INT`/`BIGINT`)에서만 유효. DDL: PG·Oracle `GENERATED BY DEFAULT AS IDENTITY`, MySQL `AUTO_INCREMENT`, MSSQL `IDENTITY(1,1)`.
- DDL 출력: CREATE TABLE(컬럼 타입·NOT NULL·DEFAULT·자동증가·PK) → FK(1:1은 UNIQUE 제약 포함) → 인덱스 → 코멘트(논리명[+설명], 방언별 문법). 식별자는 Phase 1에서 **인용 없이** 물리명 원문으로 출력(가독성 우선; 예약어/특수문자 인용은 후속).
- 범위: 전체 / 그룹 단위 / 선택 테이블.
- 결정적 출력: 테이블은 물리명 오름차순, 컬럼은 `order` 오름차순. 같은 입력 → 같은 DDL(테스트 골든 비교 가능).
- 순수 함수·불변. 웹은 core 문자열을 표시만 한다.

## 범위 밖

- 도메인 기반 타입 해석·CHECK 제약(Phase 2), Excel 산출물(Phase 2), 이미지 내보내기(M8), DDL 역설계(Phase 4), 식별자 인용/예약어 회피.

---

## File Structure

**core (수정/신규):**
- `packages/core/src/logical-type.ts` — 17종으로 확장(파서·정규화).
- `packages/core/src/dialect.ts` — `toDialectType`·`resolveColumnType` 추가(변환표).
- `packages/core/src/ddl.ts` (신규) — `generateDdl(model, dialect, scope)`.
- 신규 테스트: `logical-type.test.ts`(확장), `dialect.test.ts`, `ddl.test.ts`.
- `packages/core/src/index.ts` — 신규 export.

**web (신규):**
- `apps/web/src/editor/export-dialog.tsx` — DDL 내보내기 다이얼로그(방언·범위 선택, 미리보기, 복사·다운로드).
- 테스트: `export-dialog.test.tsx`.

**web (수정):**
- `apps/web/src/pages/project.tsx` — 헤더에 "내보내기" 버튼(다이얼로그 열기).

---

## Task 1: core 논리 타입 17종 파서·정규화

**Files:**
- Modify: `packages/core/src/logical-type.ts`
- Modify/Create: `packages/core/src/logical-type.test.ts`

**Interfaces:**
- Produces:
  ```ts
  type LogicalTypeKind =
    | 'CHAR' | 'VARCHAR' | 'TEXT' | 'SMALLINT' | 'INT' | 'BIGINT' | 'DECIMAL'
    | 'FLOAT' | 'DOUBLE' | 'BOOLEAN' | 'DATE' | 'TIME' | 'DATETIME' | 'TIMESTAMPTZ'
    | 'BLOB' | 'JSON' | 'UUID'
  type LogicalType =
    | { kind: 'CHAR' | 'VARCHAR'; length: number }
    | { kind: 'DECIMAL'; precision: number; scale: number }
    | { kind: Exclude<LogicalTypeKind, 'CHAR' | 'VARCHAR' | 'DECIMAL'> }
  type ParseResult = { ok: true; type: LogicalType; canonical: string } | { ok: false; raw: string }
  parseLogicalType(input: string): ParseResult
  ```

### Steps

- [ ] **Step 1: 실패 테스트** — `logical-type.test.ts` 전체 교체

```ts
import { describe, expect, it } from 'vitest'
import { parseLogicalType } from './logical-type.js'

const canon = (s: string) => { const r = parseLogicalType(s); return r.ok ? r.canonical : `RAW:${r.raw}` }

describe('parseLogicalType — 17종·별칭·정규화', () => {
  it('파라미터 없는 타입', () => {
    for (const t of ['TEXT', 'SMALLINT', 'INT', 'BIGINT', 'FLOAT', 'DOUBLE', 'BOOLEAN',
      'DATE', 'TIME', 'DATETIME', 'TIMESTAMPTZ', 'BLOB', 'JSON', 'UUID']) {
      expect(canon(t.toLowerCase())).toBe(t)
    }
  })
  it('길이 타입', () => {
    expect(canon('char(1)')).toBe('CHAR(1)')
    expect(canon('varchar(255)')).toBe('VARCHAR(255)')
  })
  it('DECIMAL scale 기본 0', () => {
    expect(canon('decimal(15)')).toBe('DECIMAL(15,0)')
    expect(canon('DECIMAL(18,2)')).toBe('DECIMAL(18,2)')
  })
  it('별칭 정규화', () => {
    expect(canon('integer')).toBe('INT')
    expect(canon('varchar2(100)')).toBe('VARCHAR(100)')
    expect(canon('numeric(10,2)')).toBe('DECIMAL(10,2)')
    expect(canon('number(5)')).toBe('DECIMAL(5,0)')
    expect(canon('timestamp')).toBe('DATETIME')
    expect(canon('bool')).toBe('BOOLEAN')
    expect(canon('double precision')).toBe('DOUBLE')
  })
  it('CHAR/VARCHAR 길이 누락은 실패', () => {
    expect(canon('varchar')).toBe('RAW:varchar')
    expect(canon('char')).toBe('RAW:char')
  })
  it('파라미터 없는 타입에 괄호가 오면 실패(원문 보존)', () => {
    expect(canon('int(11)')).toBe('RAW:int(11)')
  })
  it('미지의 벤더 타입은 원문 보존', () => {
    expect(canon('geometry')).toBe('RAW:geometry')
    expect(canon('')).toBe('RAW:')
  })
})
```

- [ ] **Step 2: 실패 확인** — `pnpm --filter @erdd/core test logical-type` → FAIL(3종만 지원).

- [ ] **Step 3: 구현** — `logical-type.ts` 전체 교체

```ts
export type LogicalTypeKind =
  | 'CHAR' | 'VARCHAR' | 'TEXT' | 'SMALLINT' | 'INT' | 'BIGINT' | 'DECIMAL'
  | 'FLOAT' | 'DOUBLE' | 'BOOLEAN' | 'DATE' | 'TIME' | 'DATETIME' | 'TIMESTAMPTZ'
  | 'BLOB' | 'JSON' | 'UUID'

export type LogicalType =
  | { kind: 'CHAR'; length: number }
  | { kind: 'VARCHAR'; length: number }
  | { kind: 'DECIMAL'; precision: number; scale: number }
  | { kind: Exclude<LogicalTypeKind, 'CHAR' | 'VARCHAR' | 'DECIMAL'> }

export type ParseResult =
  | { ok: true; type: LogicalType; canonical: string }
  | { ok: false; raw: string }

// 별칭 → 정규 kind. 키는 대문자·단일공백 정규화된 이름.
const ALIASES: Record<string, LogicalTypeKind> = {
  INTEGER: 'INT',
  VARCHAR2: 'VARCHAR',
  NUMERIC: 'DECIMAL',
  NUMBER: 'DECIMAL',
  TIMESTAMP: 'DATETIME',
  BOOL: 'BOOLEAN',
  'DOUBLE PRECISION': 'DOUBLE',
}
const KINDS = new Set<LogicalTypeKind>([
  'CHAR', 'VARCHAR', 'TEXT', 'SMALLINT', 'INT', 'BIGINT', 'DECIMAL',
  'FLOAT', 'DOUBLE', 'BOOLEAN', 'DATE', 'TIME', 'DATETIME', 'TIMESTAMPTZ',
  'BLOB', 'JSON', 'UUID',
])
const LENGTH_KINDS = new Set(['CHAR', 'VARCHAR'])

export function parseLogicalType(input: string): ParseResult {
  const raw = input.trim()
  const m = /^([A-Za-z][A-Za-z0-9_ ]*?)\s*(?:\(\s*(\d+)\s*(?:,\s*(\d+)\s*)?\))?$/.exec(raw)
  if (!m) return { ok: false, raw }
  const name = m[1]!.toUpperCase().replace(/\s+/g, ' ')
  const kind = (ALIASES[name] ?? (KINDS.has(name as LogicalTypeKind) ? (name as LogicalTypeKind) : undefined))
  if (!kind) return { ok: false, raw }
  const p1 = m[2] === undefined ? undefined : Number(m[2])
  const p2 = m[3] === undefined ? undefined : Number(m[3])

  if (kind === 'DECIMAL') {
    if (p1 === undefined) return { ok: false, raw }
    const scale = p2 ?? 0
    return { ok: true, type: { kind: 'DECIMAL', precision: p1, scale }, canonical: `DECIMAL(${p1},${scale})` }
  }
  if (LENGTH_KINDS.has(kind)) {
    if (p1 === undefined || p2 !== undefined) return { ok: false, raw }
    return { ok: true, type: { kind, length: p1 } as LogicalType, canonical: `${kind}(${p1})` }
  }
  // 파라미터 없는 타입: 괄호가 오면 실패.
  if (p1 !== undefined) return { ok: false, raw }
  return { ok: true, type: { kind } as LogicalType, canonical: kind }
}
```

- [ ] **Step 4: 통과 확인** — `pnpm --filter @erdd/core test logical-type` → PASS.

- [ ] **Step 5: index.ts export 갱신** — `LogicalTypeKind` 타입 추가 export(기존 `LogicalType`/`ParseResult`/`parseLogicalType` 유지).

```ts
export { parseLogicalType } from './logical-type.js'
export type { LogicalType, LogicalTypeKind, ParseResult } from './logical-type.js'
```

- [ ] **Step 6: 전체 검증** — `pnpm --filter @erdd/core test && pnpm --filter @erdd/core typecheck` → PASS.

- [ ] **Step 7: 커밋**

```bash
git add packages/core/src/logical-type.ts packages/core/src/logical-type.test.ts packages/core/src/index.ts
git commit -m "feat(core): 논리 타입 17종 파서·정규화 완성"
```

---

## Task 2: core 방언 변환표

**Files:**
- Modify: `packages/core/src/dialect.ts`, `packages/core/src/index.ts`
- Create: `packages/core/src/dialect.test.ts`

**Interfaces:**
- Consumes: `LogicalType`, `parseLogicalType` (Task 1).
- Produces:
  ```ts
  // 논리 타입 → 방언 물리 타입 문자열.
  toDialectType(type: LogicalType, dialect: Dialect): string
  // 원문 타입 문자열 → { sql, warning? }. 파싱 실패 시 원문 그대로(sql=raw). Oracle TIME/DATETIME/JSON 등 경고.
  resolveColumnType(rawType: string, dialect: Dialect): { sql: string; warning?: string }
  ```

### 변환표 (읽고 시작 — 14-domain 그대로)

아래 코드에 전량 수록. 파라미터 타입은 n/p/s를 치환한다.

### Steps

- [ ] **Step 1: 실패 테스트** — `dialect.test.ts`

```ts
import { describe, expect, it } from 'vitest'
import { toDialectType, resolveColumnType } from './dialect.js'
import { parseLogicalType } from './logical-type.js'

const conv = (t: string, d: Parameters<typeof resolveColumnType>[1]) => {
  const p = parseLogicalType(t)
  if (!p.ok) throw new Error('parse fail')
  return toDialectType(p.type, d)
}

describe('toDialectType', () => {
  it('문자열 타입', () => {
    expect(conv('VARCHAR(100)', 'postgresql')).toBe('varchar(100)')
    expect(conv('VARCHAR(100)', 'oracle')).toBe('VARCHAR2(100)')
    expect(conv('VARCHAR(100)', 'mssql')).toBe('NVARCHAR(100)')
    expect(conv('CHAR(1)', 'mssql')).toBe('NCHAR(1)')
    expect(conv('TEXT', 'oracle')).toBe('CLOB')
    expect(conv('TEXT', 'mssql')).toBe('NVARCHAR(MAX)')
  })
  it('정수/실수', () => {
    expect(conv('INT', 'oracle')).toBe('NUMBER(10)')
    expect(conv('BIGINT', 'oracle')).toBe('NUMBER(19)')
    expect(conv('DECIMAL(15,2)', 'postgresql')).toBe('numeric(15,2)')
    expect(conv('DECIMAL(15,2)', 'oracle')).toBe('NUMBER(15,2)')
    expect(conv('DOUBLE', 'postgresql')).toBe('double precision')
    expect(conv('BOOLEAN', 'mysql')).toBe('TINYINT(1)')
    expect(conv('BOOLEAN', 'oracle')).toBe('NUMBER(1)')
  })
  it('일시/기타', () => {
    expect(conv('DATETIME', 'postgresql')).toBe('timestamp')
    expect(conv('DATETIME', 'oracle')).toBe('DATE')
    expect(conv('TIMESTAMPTZ', 'oracle')).toBe('TIMESTAMP WITH TIME ZONE')
    expect(conv('UUID', 'postgresql')).toBe('uuid')
    expect(conv('UUID', 'oracle')).toBe('RAW(16)')
    expect(conv('JSON', 'postgresql')).toBe('jsonb')
  })
})

describe('resolveColumnType', () => {
  it('파싱 성공은 방언 타입', () => {
    expect(resolveColumnType('varchar(50)', 'postgresql')).toEqual({ sql: 'varchar(50)' })
  })
  it('파싱 실패는 원문 그대로', () => {
    expect(resolveColumnType('geometry', 'postgresql')).toEqual({ sql: 'geometry' })
  })
  it('Oracle TIME은 TIMESTAMP + 경고', () => {
    const r = resolveColumnType('TIME', 'oracle')
    expect(r.sql).toBe('TIMESTAMP')
    expect(r.warning).toBeTruthy()
  })
})
```

- [ ] **Step 2: 실패 확인** — `pnpm --filter @erdd/core test dialect` → FAIL.

- [ ] **Step 3: 구현** — `dialect.ts` 확장(기존 `DIALECTS`/`Dialect` 유지, 아래 추가)

```ts
import { parseLogicalType, type LogicalType, type LogicalTypeKind } from './logical-type.js'

export const DIALECTS = ['postgresql', 'mysql', 'oracle', 'mssql'] as const
export type Dialect = (typeof DIALECTS)[number]

// 파라미터 없는 kind의 방언별 고정 타입. 파라미터 타입(CHAR/VARCHAR/DECIMAL)은 아래 함수에서 조합.
const FIXED: Record<Exclude<LogicalTypeKind, 'CHAR' | 'VARCHAR' | 'DECIMAL'>, Record<Dialect, string>> = {
  TEXT: { postgresql: 'text', mysql: 'LONGTEXT', oracle: 'CLOB', mssql: 'NVARCHAR(MAX)' },
  SMALLINT: { postgresql: 'smallint', mysql: 'SMALLINT', oracle: 'NUMBER(5)', mssql: 'SMALLINT' },
  INT: { postgresql: 'integer', mysql: 'INT', oracle: 'NUMBER(10)', mssql: 'INT' },
  BIGINT: { postgresql: 'bigint', mysql: 'BIGINT', oracle: 'NUMBER(19)', mssql: 'BIGINT' },
  FLOAT: { postgresql: 'real', mysql: 'FLOAT', oracle: 'BINARY_FLOAT', mssql: 'REAL' },
  DOUBLE: { postgresql: 'double precision', mysql: 'DOUBLE', oracle: 'BINARY_DOUBLE', mssql: 'FLOAT' },
  BOOLEAN: { postgresql: 'boolean', mysql: 'TINYINT(1)', oracle: 'NUMBER(1)', mssql: 'BIT' },
  DATE: { postgresql: 'date', mysql: 'DATE', oracle: 'DATE', mssql: 'DATE' },
  TIME: { postgresql: 'time', mysql: 'TIME', oracle: 'TIMESTAMP', mssql: 'TIME' },
  DATETIME: { postgresql: 'timestamp', mysql: 'DATETIME', oracle: 'DATE', mssql: 'DATETIME2' },
  TIMESTAMPTZ: { postgresql: 'timestamptz', mysql: 'TIMESTAMP', oracle: 'TIMESTAMP WITH TIME ZONE', mssql: 'DATETIMEOFFSET' },
  BLOB: { postgresql: 'bytea', mysql: 'LONGBLOB', oracle: 'BLOB', mssql: 'VARBINARY(MAX)' },
  JSON: { postgresql: 'jsonb', mysql: 'JSON', oracle: 'CLOB', mssql: 'NVARCHAR(MAX)' },
  UUID: { postgresql: 'uuid', mysql: 'CHAR(36)', oracle: 'RAW(16)', mssql: 'UNIQUEIDENTIFIER' },
}

export function toDialectType(type: LogicalType, dialect: Dialect): string {
  if (type.kind === 'CHAR') {
    return dialect === 'mssql' ? `NCHAR(${type.length})` : `CHAR(${type.length})`
  }
  if (type.kind === 'VARCHAR') {
    const n = type.length
    return dialect === 'postgresql' ? `varchar(${n})`
      : dialect === 'mysql' ? `VARCHAR(${n})`
      : dialect === 'oracle' ? `VARCHAR2(${n})`
      : `NVARCHAR(${n})`
  }
  if (type.kind === 'DECIMAL') {
    const ps = `${type.precision},${type.scale}`
    return dialect === 'postgresql' ? `numeric(${ps})`
      : dialect === 'oracle' ? `NUMBER(${ps})`
      : `DECIMAL(${ps})`
  }
  return FIXED[type.kind][dialect]
}

// Oracle에서 손실/변환 경고가 필요한 조합.
const ORACLE_WARN: Partial<Record<LogicalTypeKind, string>> = {
  TIME: 'Oracle에는 TIME이 없어 TIMESTAMP로 변환됩니다',
  DATETIME: 'Oracle DATE는 초 단위까지만 지원합니다(밀리초 필요 시 도메인에서 TIMESTAMP)',
  JSON: 'Oracle에서는 CLOB으로 변환됩니다(21c+ JSON 타입은 도메인 오버라이드)',
}

export function resolveColumnType(rawType: string, dialect: Dialect): { sql: string; warning?: string } {
  const parsed = parseLogicalType(rawType)
  if (!parsed.ok) return { sql: parsed.raw }
  const sql = toDialectType(parsed.type, dialect)
  if (dialect === 'oracle') {
    const warning = ORACLE_WARN[parsed.type.kind]
    if (warning) return { sql, warning }
  }
  return { sql }
}
```

- [ ] **Step 4: 통과 확인** — `pnpm --filter @erdd/core test dialect` → PASS.

- [ ] **Step 5: index.ts export**

```ts
export { DIALECTS, toDialectType, resolveColumnType } from './dialect.js'
export type { Dialect } from './dialect.js'
```
(기존 `DIALECTS`/`Dialect` export 줄을 이 줄로 대체 — 중복 export 주의.)

- [ ] **Step 6: 전체 검증·커밋**

```bash
git add packages/core/src/dialect.ts packages/core/src/dialect.test.ts packages/core/src/index.ts
git commit -m "feat(core): 4방언 타입 변환표·resolveColumnType"
```

---

## Task 3: core DDL 생성기 — CREATE TABLE

**Files:**
- Create: `packages/core/src/ddl.ts`, `packages/core/src/ddl.test.ts`
- Modify: `packages/core/src/index.ts`

**Interfaces:**
- Consumes: `ProjectModel`, `Column`, `Table`, `resolveColumnType`, `Dialect`.
- Produces:
  ```ts
  type DdlScope = { kind: 'all' } | { kind: 'group'; groupId: string } | { kind: 'tables'; tableIds: string[] }
  generateDdl(model: ProjectModel, dialect: Dialect, scope?: DdlScope): string  // 기본 scope = all
  ```

### 설계 (읽고 시작)

- 이 태스크는 **CREATE TABLE 블록만** 생성한다(FK·인덱스·코멘트는 Task 4에서 append). 반환 문자열은 CREATE TABLE들만 포함하고, Task 4가 같은 함수에 이어붙인다.
- 대상 테이블: scope로 선택(all=전체, group=groupId 소속, tables=id 목록). 물리명 오름차순 정렬.
- 각 테이블: 컬럼(order 오름차순) 각각 `  <물리명> <타입>[ 자동증가][ NOT NULL][ DEFAULT <값>]`.
  - 타입: `resolveColumnType(col.type, dialect).sql`.
  - 자동증가: `col.autoIncrement && col.isPk && 정수형(INT/SMALLINT/BIGINT)`일 때만. 방언별 토큰. 자동증가면 DEFAULT는 생략.
  - NOT NULL: `!col.nullable`.
  - DEFAULT: `col.defaultValue !== null && col.defaultValue !== ''`이고 자동증가가 아니면 `DEFAULT <원문>`.
- PK: isPk 컬럼(order 순)이 있으면 마지막 요소로 `  PRIMARY KEY (<물리명들>)`.
- 컬럼/PK 요소를 `,\n`로 연결, `CREATE TABLE <물리명> (\n...\n);` 형식. 테이블 사이 빈 줄 1개.
- 정수형 판정: `parseLogicalType(col.type)`가 ok이고 kind ∈ {SMALLINT, INT, BIGINT}.

### Steps

- [ ] **Step 1: 실패 테스트** — `ddl.test.ts`

```ts
import { describe, expect, it } from 'vitest'
import { generateDdl } from './ddl.js'
import { createEmptyModel, type ProjectModel, type Table, type Column } from './model.js'

function tbl(id: string, physicalName: string, over: Partial<Table> = {}): Table {
  return { id, logicalName: physicalName, physicalName, comment: null, groupId: null,
    position: { x: 0, y: 0 }, groupPosition: null, ...over }
}
function col(id: string, tableId: string, physicalName: string, type: string, over: Partial<Column> = {}): Column {
  return { id, tableId, logicalName: physicalName, physicalName, type, isPk: false,
    autoIncrement: false, nullable: true, defaultValue: null, order: 0, comment: null, ...over }
}

function usersModel(): ProjectModel {
  const m = createEmptyModel()
  m.tables['t'] = tbl('t', 'USERS')
  m.columns['c1'] = col('c1', 't', 'ID', 'BIGINT', { isPk: true, nullable: false, autoIncrement: true, order: 0 })
  m.columns['c2'] = col('c2', 't', 'NAME', 'VARCHAR(100)', { nullable: false, order: 1 })
  m.columns['c3'] = col('c3', 't', 'STATUS', 'CHAR(1)', { defaultValue: "'A'", order: 2 })
  return m
}

describe('generateDdl — CREATE TABLE', () => {
  it('PostgreSQL', () => {
    expect(generateDdl(usersModel(), 'postgresql')).toBe(
`CREATE TABLE USERS (
  ID bigint GENERATED BY DEFAULT AS IDENTITY NOT NULL,
  NAME varchar(100) NOT NULL,
  STATUS char(1) DEFAULT 'A',
  PRIMARY KEY (ID)
);`)
  })
  it('MySQL 자동증가·타입', () => {
    const ddl = generateDdl(usersModel(), 'mysql')
    expect(ddl).toContain('ID BIGINT AUTO_INCREMENT NOT NULL')
    expect(ddl).toContain('NAME VARCHAR(100) NOT NULL')
    expect(ddl).toContain('PRIMARY KEY (ID)')
  })
  it('Oracle 타입 변환', () => {
    const ddl = generateDdl(usersModel(), 'oracle')
    expect(ddl).toContain('ID NUMBER(19) GENERATED BY DEFAULT AS IDENTITY NOT NULL')
    expect(ddl).toContain('NAME VARCHAR2(100) NOT NULL')
  })
  it('범위: 선택 테이블만', () => {
    const m = usersModel()
    m.tables['t2'] = tbl('t2', 'LOGS')
    m.columns['x'] = col('x', 't2', 'MSG', 'TEXT', { order: 0 })
    const ddl = generateDdl(m, 'postgresql', { kind: 'tables', tableIds: ['t2'] })
    expect(ddl).toContain('CREATE TABLE LOGS')
    expect(ddl).not.toContain('CREATE TABLE USERS')
  })
  it('미지 타입은 원문 유지', () => {
    const m = createEmptyModel()
    m.tables['t'] = tbl('t', 'GEO')
    m.columns['c'] = col('c', 't', 'SHAPE', 'geometry', { order: 0 })
    expect(generateDdl(m, 'postgresql')).toContain('SHAPE geometry')
  })
})
```

- [ ] **Step 2: 실패 확인** — `pnpm --filter @erdd/core test ddl` → FAIL.

- [ ] **Step 3: 구현** — `ddl.ts`

```ts
import type { Column, ProjectModel, Table } from './model.js'
import { resolveColumnType, type Dialect } from './dialect.js'
import { parseLogicalType } from './logical-type.js'

export type DdlScope =
  | { kind: 'all' }
  | { kind: 'group'; groupId: string }
  | { kind: 'tables'; tableIds: string[] }

const INT_KINDS = new Set(['SMALLINT', 'INT', 'BIGINT'])

function isIntegerType(type: string): boolean {
  const p = parseLogicalType(type)
  return p.ok && INT_KINDS.has(p.type.kind)
}

function autoIncrementToken(dialect: Dialect): string {
  switch (dialect) {
    case 'postgresql':
    case 'oracle':
      return 'GENERATED BY DEFAULT AS IDENTITY'
    case 'mysql':
      return 'AUTO_INCREMENT'
    case 'mssql':
      return 'IDENTITY(1,1)'
  }
}

function selectTables(model: ProjectModel, scope: DdlScope): Table[] {
  const all = Object.values(model.tables)
  let picked: Table[]
  if (scope.kind === 'all') picked = all
  else if (scope.kind === 'group') picked = all.filter((t) => t.groupId === scope.groupId)
  else {
    const ids = new Set(scope.tableIds)
    picked = all.filter((t) => ids.has(t.id))
  }
  return picked.sort((a, b) => a.physicalName.localeCompare(b.physicalName))
}

function tableColumns(model: ProjectModel, tableId: string): Column[] {
  return Object.values(model.columns).filter((c) => c.tableId === tableId).sort((a, b) => a.order - b.order)
}

function columnLine(col: Column, dialect: Dialect): string {
  const parts = [col.physicalName, resolveColumnType(col.type, dialect).sql]
  const auto = col.autoIncrement && col.isPk && isIntegerType(col.type)
  if (auto) parts.push(autoIncrementToken(dialect))
  if (!col.nullable) parts.push('NOT NULL')
  if (!auto && col.defaultValue !== null && col.defaultValue !== '') parts.push(`DEFAULT ${col.defaultValue}`)
  return `  ${parts.join(' ')}`
}

export function generateDdl(model: ProjectModel, dialect: Dialect, scope: DdlScope = { kind: 'all' }): string {
  const tables = selectTables(model, scope)
  const blocks = tables.map((table) => {
    const cols = tableColumns(model, table.id)
    const lines = cols.map((c) => columnLine(c, dialect))
    const pks = cols.filter((c) => c.isPk)
    if (pks.length > 0) lines.push(`  PRIMARY KEY (${pks.map((c) => c.physicalName).join(', ')})`)
    return `CREATE TABLE ${table.physicalName} (\n${lines.join(',\n')}\n);`
  })
  return blocks.join('\n\n')
}
```

- [ ] **Step 4: 통과 확인·index export·커밋**

```bash
# index.ts: export { generateDdl } from './ddl.js'; export type { DdlScope } from './ddl.js'
git add packages/core/src/ddl.ts packages/core/src/ddl.test.ts packages/core/src/index.ts
git commit -m "feat(core): DDL 생성기 — CREATE TABLE·타입·자동증가·PK·범위"
```

---

## Task 4: core DDL 생성기 — FK·인덱스·코멘트

**Files:**
- Modify: `packages/core/src/ddl.ts`, `packages/core/src/ddl.test.ts`

**Interfaces:** `generateDdl` 확장(시그니처 동일). CREATE TABLE 뒤에 FK·인덱스·코멘트 문을 이어붙인다.

### 설계 (읽고 시작)

- 선택된 테이블 집합(scope) 기준. **선택 테이블에 속한 것만** 출력한다(FK는 양 끝이 모두 선택 집합에 있을 때만; 인덱스·코멘트는 소속 테이블이 선택 집합에 있을 때).
- 출력 순서: CREATE TABLE들 → FK → 인덱스 → 코멘트(MySQL 제외; MySQL 코멘트는 인라인). 각 섹션 사이 빈 줄.
- **FK**: 관계마다 `ALTER TABLE <child> ADD CONSTRAINT <name> FOREIGN KEY (<childCols>) REFERENCES <parent> (<parentCols>);`. childCols/parentCols는 `columnMappings` 순서. 제약명 = `rel.name` 있으면 그것, 없으면 `FK_<child>_<parent>`(물리명). 1:1 관계는 추가로 `ALTER TABLE <child> ADD CONSTRAINT UQ_<child>_<...> UNIQUE (<childCols>);`.
- **인덱스**: `CREATE [UNIQUE ]INDEX <name> ON <table> (<col dir, ...>);`. dir은 `asc`/`desc`→대문자. 이름은 index.name.
- **코멘트**: 대상 = 논리명 + (설명 있으면 ` - 설명`). 작은따옴표 이스케이프(`'`→`''`).
  - PostgreSQL·Oracle: `COMMENT ON TABLE <t> IS '...';`, `COMMENT ON COLUMN <t>.<c> IS '...';`.
  - MSSQL: `EXEC sys.sp_addextendedproperty @name=N'MS_Description', @value=N'...', @level0type=N'SCHEMA', @level0name=N'dbo', @level1type=N'TABLE', @level1name=N'<t>'[, @level2type=N'COLUMN', @level2name=N'<c>'];`.
  - MySQL: **인라인**. 컬럼 정의 끝에 ` COMMENT '...'`, 테이블은 `) COMMENT='...'`로 CREATE TABLE에 포함. → MySQL은 Task 3의 columnLine·CREATE TABLE 조립에 코멘트를 끼워야 하므로, `dialect==='mysql'`일 때 columnLine에 코멘트 인라인을 추가하고 CREATE TABLE 닫는 `)` 뒤에 테이블 코멘트를 붙인다. 비-MySQL은 별도 문 섹션.
- 논리명==물리명이고 설명이 없으면 코멘트를 생략(노이즈 방지). 논리명이 있거나 설명이 있으면 출력.

### Steps

- [ ] **Step 1: 테스트 추가** — `ddl.test.ts`에 FK·인덱스·코멘트 케이스

```ts
// 관계·인덱스·코멘트가 있는 모델
function relModel(): ProjectModel {
  const m = createEmptyModel()
  m.tables['p'] = tbl('p', 'USERS', { logicalName: '회원', comment: '회원 마스터' })
  m.tables['c'] = tbl('c', 'ORDERS', { logicalName: '주문' })
  m.columns['pk'] = col('pk', 'p', 'ID', 'BIGINT', { isPk: true, nullable: false, order: 0, logicalName: '아이디' })
  m.columns['fk'] = col('fk', 'c', 'USER_ID', 'BIGINT', { nullable: false, order: 0 })
  m.columns['co'] = col('co', 'c', 'ID', 'BIGINT', { isPk: true, nullable: false, order: 1 })
  m.relationships['r'] = { id: 'r', parentTableId: 'p', childTableId: 'c',
    columnMappings: [{ childColumnId: 'fk', parentColumnId: 'pk' }],
    cardinality: '1:N', identifying: false, name: null }
  m.indexes['ix'] = { id: 'ix', tableId: 'c', name: 'IX_ORDERS_USER', unique: false,
    columns: [{ columnId: 'fk', direction: 'asc' }] }
  return m
}

describe('generateDdl — FK·인덱스·코멘트', () => {
  it('PostgreSQL FK·인덱스·코멘트', () => {
    const ddl = generateDdl(relModel(), 'postgresql')
    expect(ddl).toContain('ALTER TABLE ORDERS ADD CONSTRAINT FK_ORDERS_USERS FOREIGN KEY (USER_ID) REFERENCES USERS (ID);')
    expect(ddl).toContain('CREATE INDEX IX_ORDERS_USER ON ORDERS (USER_ID ASC);')
    expect(ddl).toContain("COMMENT ON TABLE USERS IS '회원 - 회원 마스터';")
    expect(ddl).toContain("COMMENT ON COLUMN USERS.ID IS '아이디';")
  })
  it('1:1 관계는 UNIQUE 제약 포함', () => {
    const m = relModel()
    m.relationships['r']!.cardinality = '1:1'
    const ddl = generateDdl(m, 'postgresql')
    expect(ddl).toContain('UNIQUE (USER_ID)')
  })
  it('MySQL 인라인 코멘트', () => {
    const ddl = generateDdl(relModel(), 'mysql')
    expect(ddl).toContain("COMMENT '회원 - 회원 마스터'") // 테이블
    expect(ddl).not.toContain('COMMENT ON')
  })
  it('MSSQL 확장 속성', () => {
    const ddl = generateDdl(relModel(), 'mssql')
    expect(ddl).toContain('sp_addextendedproperty')
  })
  it('FK는 양 끝이 선택 범위에 있을 때만', () => {
    const ddl = generateDdl(relModel(), 'postgresql', { kind: 'tables', tableIds: ['c'] })
    expect(ddl).not.toContain('ADD CONSTRAINT FK_') // 부모 USERS 미포함
  })
})
```

- [ ] **Step 2: 실패 확인** — FAIL.

- [ ] **Step 3: 구현** — `ddl.ts` 확장. columnLine/CREATE TABLE에 MySQL 인라인 코멘트를 넣고, 비-MySQL 코멘트/FK/인덱스 섹션을 append.

핵심 헬퍼(전체 구현은 아래):
```ts
function commentText(logicalName: string, physicalName: string, comment: string | null): string | null {
  const parts: string[] = []
  if (logicalName && logicalName !== physicalName) parts.push(logicalName)
  else if (logicalName && logicalName === physicalName && comment) parts.push(logicalName)
  if (comment) parts.push(comment)
  if (parts.length === 0) return null
  return parts.join(' - ')
}
const esc = (s: string) => s.replace(/'/g, "''")

function fkName(rel: { name: string | null }, child: string, parent: string): string {
  return rel.name && rel.name.trim() !== '' ? rel.name : `FK_${child}_${parent}`
}
```

`columnLine`을 dialect==='mysql'일 때 코멘트 인라인을 붙이도록 확장하고, CREATE TABLE 조립에서 MySQL이면 닫는 괄호 뒤 테이블 코멘트를 붙인다. 그 뒤 `generateDdl`에서 섹션들을 순서대로 조립:
1. createBlocks (Task 3, MySQL이면 코멘트 인라인 포함)
2. fkStatements (선택 집합의 부모·자식 모두 포함하는 관계만; 1:1이면 UNIQUE 추가)
3. indexStatements (소속 테이블이 선택 집합)
4. commentStatements (비-MySQL만; 테이블·컬럼)

`[createBlocks.join('\n\n'), fk, index, comment].filter(s => s.trim() !== '').join('\n\n')`.

(구현자: Task 3의 `columnLine`·CREATE TABLE 조립 함수에 `model`/코멘트 접근이 필요하므로 시그니처를 조정하되, Task 3 테스트가 계속 통과해야 한다 — 비-MySQL·코멘트 없는 케이스의 출력은 불변.)

- [ ] **Step 4: 통과 확인** — `pnpm --filter @erdd/core test ddl` → Task 3+4 전체 PASS. `pnpm --filter @erdd/core test && typecheck` → 전체 PASS.

- [ ] **Step 5: 커밋**

```bash
git add packages/core/src/ddl.ts packages/core/src/ddl.test.ts
git commit -m "feat(core): DDL 생성기 — FK·1:1 UNIQUE·인덱스·방언별 코멘트"
```

---

## Task 5: 웹 DDL 내보내기 다이얼로그

**Files:**
- Create: `apps/web/src/editor/export-dialog.tsx`, `apps/web/src/editor/export-dialog.test.tsx`
- Modify: `apps/web/src/pages/project.tsx`

**Interfaces:**
- Consumes: `generateDdl`, `DIALECTS`, `type Dialect`, `type DdlScope`(core); store `model`/`activeGroupView`; shadcn `Dialog`/`Button`/`Select`.

### 설계 (읽고 시작)

- 헤더 "내보내기" 버튼 → 다이얼로그. 방언 선택(4개 버튼/토글), 범위 선택(전체 / 현재 그룹 뷰(activeGroupView 있으면) / — 선택 테이블은 다중선택 미구현이라 Phase 1에서는 전체·그룹만), 미리보기(monospace `<pre>`, 읽기전용), "복사"·"다운로드(.sql)" 버튼.
- DDL은 `useMemo(() => generateDdl(model, dialect, scope), [model, dialect, scope])`로 계산.
- 복사: `navigator.clipboard.writeText`. 다운로드: Blob + a[download] (`<projectName>_<dialect>.sql`).
- 다이얼로그가 없으면(shadcn dialog 미설치) 간단한 오버레이로 구현하거나 기존 dialog 컴포넌트 확인 후 사용. project-settings/프로젝트 생성에서 쓰는 다이얼로그 패턴 재사용.

### Steps

- [ ] **Step 1: export-dialog.tsx 구현** — 방언·범위·미리보기·복사·다운로드. (기존 프로젝트 생성 다이얼로그의 shadcn `Dialog` 패턴을 읽고 동일 컴포넌트 사용.)

- [ ] **Step 2: project.tsx 헤더에 "내보내기" 버튼** — 다이얼로그 오픈. `loaded`일 때만.

- [ ] **Step 3: export-dialog.test.tsx** — 최소 2개: (a) 다이얼로그 열면 기본 방언의 CREATE TABLE 미리보기가 렌더된다(store에 buildSampleModel 적재), (b) 방언을 바꾸면 미리보기가 그 방언 타입으로 갱신된다(예: oracle 선택 시 VARCHAR2 등장). tRPC 불필요(순수 core + store) — QueryClient/provider 없이 store만 세팅해 렌더 가능하면 그렇게, 아니면 기존 provider 패턴 사용.

- [ ] **Step 4: 웹 테스트·타입체크·커밋**

```bash
git add apps/web/src/editor/export-dialog.tsx apps/web/src/editor/export-dialog.test.tsx apps/web/src/pages/project.tsx
git commit -m "feat(web): DDL 내보내기 다이얼로그 — 방언·범위·복사·다운로드"
```

---

## 완료 기준 (최종 리뷰 체크리스트)

- 논리 타입 17종이 파싱·정규화되고, 미지 타입은 원문 보존된다.
- 4방언 변환이 14-domain 표와 일치한다(대표 케이스 골든 확인).
- DDL: CREATE TABLE(타입·NOT NULL·DEFAULT·자동증가·PK), FK(1:1 UNIQUE 포함), 인덱스, 코멘트(방언별 문법)가 결정적으로 생성된다.
- 범위(전체/그룹)가 반영되고, FK는 양 끝이 범위에 있을 때만 나온다.
- 에디터 헤더 "내보내기"로 방언·범위를 골라 미리보기·복사·다운로드가 동작한다.
- 방언 변경 시 미리보기가 즉시 갱신된다.
- 전체 테스트(core+server+web) 통과, 타입체크 클린.

## 이월(후속)

식별자 인용/예약어 회피, 도메인 기반 타입·CHECK 제약(Phase 2), 선택 테이블 범위(다중 선택 도입 후), Excel 산출물(Phase 2), 이미지 내보내기(M8).
