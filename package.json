{
  "name": "erdd",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "dev": "set -a; if [ -f .env ]; then . ./.env; else echo '⚠️  .env 가 없습니다 — .env.example 을 복사해 채우세요(DATABASE_URL 없이 뜨면 모든 tRPC 가 412).' >&2; fi; set +a; pnpm --parallel -r dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm -r typecheck && pnpm -C packages/core test && pnpm -C packages/cli test && pnpm -C apps/web test && pnpm -C apps/server test"
  }
}
