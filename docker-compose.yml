services:
  db:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: erdd
      POSTGRES_DB: erdd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
